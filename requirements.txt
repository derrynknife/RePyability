# Runtime dependencies are declared in pyproject.toml ([project.dependencies]).
# Installing the package (below) pulls them in, keeping a single source of
# truth. For a plain runtime install use `pip install .` (or `pip install
# repyability` from PyPI).
.
